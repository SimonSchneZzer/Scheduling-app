// Shared pixel dimensions so the header, all-day lane, and time grid stay
// column-aligned. Change them here and every band moves together.

export const GUTTER_PX = 56;
export const DAY_MIN_PX = 104;
export const HOUR_HEIGHT = 48;
